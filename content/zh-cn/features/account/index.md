---
llms_section: "Account zh-CN"
llms_order: 1820
llms_summary: "当你需要用简体中文了解 Raft 账号与服务器的区别、账号上哪些能改（显示名、头像、登录方式）、哪些不能改（用户名），以及如何申请删除时阅读。"
title: 账号
description: 你的 Raft 账号——用户名与显示名、头像、登录方式，以及如何申请删除数据。
---

# 账号

你的 Raft 账号代表你这个人：一个身份，可以同时属于多台服务器。它和服务器本身是分开的——服务器有自己的成员、频道和数据，**离开或失去某台服务器的访问权，并不会删除你的账号**。

下面这些都在 **Settings** 的 **Account** 里。

## 用户名与显示名

这是两个东西，而且只有一个能改。

- **用户名**：你的 `@name`。它在 Raft 全站唯一，@提及和链接都指向它。**账号创建时选定，之后不能再改**，所以选一个你愿意一直用的。
- **显示名**：别人在你消息旁边真正读到的名字。随时可改，不影响你的 `@name`，也不影响别人已经发出的提及。

## 头像

上传图片，**最大 5 MB**。会显示在你的消息旁边和成员列表里。

## 登录方式

你的账号可以有密码、一个或多个第三方登录方式，或者两者都有。你可以再连接一个、断开不再使用的，也可以设置或修改密码。

如果你是用第三方登录注册的、从没设过密码，Raft 可以给你发一封设置密码的邮件——**建议在断开该登录方式之前先设好**，免得把自己唯一的入口断掉。

**断开第三方登录只改变你怎么登录**，不会删除你的账号、资料或你写过的任何东西。

## 哪些属于你，哪些属于服务器

|  | 属于你 | 属于服务器 |
|---|:--:|:--:|
| 你的登录身份与个人资料 | ✓ | |
| 你在某台服务器的成员身份 | | ✓ |
| 你在服务器频道里发过的消息 | | ✓ |
| 频道、任务与线程 | | ✓ |
| 在服务器里创建的 Agent | | ✓ |

正因为这样，**离开服务器不会抹掉你在那里的历史**：那些对话属于那台服务器和它的其他成员，不属于你的账号。

## 删除账号

Raft 目前没有自助删除功能——App 里没有这个按钮。如果你想删除账号或你的数据，写信到 `contact@raft.build`，我们来处理。
